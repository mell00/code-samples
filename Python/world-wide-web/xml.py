##PYTHON XML PROCESSING SUPPORT##

#node package

node.nodeType

node.parentNode

node.attributes

node.previousSibling

node.nextSibling

node.childNodes

node.firstChild

node.lastChild

node.localName

node.prefix

node.namespaceURI

node.nodeName

node.nodeValue

node.hasAttributes()

node.hasChildNodes()

node.isSameNode(other)

node.appendChild(new_child)

node.insertBefore(new_child,reference_child)

node.removeChild(old_child)

node.replaceChild(new_child,old_child)

node.normalize()

node.cloneNode(deep)
